<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>
<?import org.kordamp.ikonli.javafx.FontIcon?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.financetracker.financetracker.MainController">

    <!-- TOPBAR -->
    <top>
        <HBox styleClass="top-bar">
            <Label text="💰 Finance Tracker" />
            <Region HBox.hgrow="ALWAYS"/>
            <Label text="📅 20 maja 2025" />
        </HBox>
    </top>

    <!-- SIDEBAR -->
    <left>
        <VBox styleClass="sidebar" alignment="TOP_CENTER">
            <Button styleClass="sidebar-button">
                <graphic>
                    <FontIcon iconLiteral="mdi-home" styleClass="sidebar-icon"/>
                </graphic>
            </Button>
            <Button styleClass="sidebar-button">
                <graphic>
                    <FontIcon iconLiteral="mdi-cash" styleClass="sidebar-icon"/>
                </graphic>
            </Button>
            <Button styleClass="sidebar-button">
                <graphic>
                    <FontIcon iconLiteral="mdi-chart-bar" styleClass="sidebar-icon"/>
                </graphic>
            </Button>
            <Button styleClass="sidebar-button">
                <graphic>
                    <FontIcon iconLiteral="mdi-wrench" styleClass="sidebar-icon"/>
                </graphic>
            </Button>
        </VBox>
    </left>

    <!-- MAIN CONTENT -->
    <center>
        <VBox alignment="CENTER" spacing="30">
            <VBox styleClass="form-card">
                <Label text="Nowa transakcja" style="-fx-font-size: 16px; -fx-font-weight: bold;" />
                <TextField fx:id="descriptionField" promptText="Opis transakcji" />
                <TextField fx:id="amountField" promptText="Kwota (+/-)" />
                <ComboBox fx:id="categoryComboBox" promptText="Kategoria" prefWidth="200"/>
                <Button text="Dodaj transakcję" onAction="#handleAddTransaction" />
            </VBox>

            <VBox spacing="10" alignment="CENTER">
                <Label fx:id="balanceLabel" text="Saldo: 0.00" style="-fx-font-size: 16px; -fx-font-weight: bold;" />
                <ListView fx:id="transactionList" prefHeight="200" prefWidth="500" />
            </VBox>
        </VBox>
    </center>
</BorderPane>